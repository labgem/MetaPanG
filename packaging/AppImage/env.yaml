name: base
channels:
  - conda-forge
  - bioconda
dependencies:
  - python >=3.11
  - pip
  # non-pip dependencies (bundled binary tools); the remaining PyPI
  # dependencies are installed by `pip install ../..` in build.sh
  - graph-tool >=2.98
  - metagraph
  - ppanggolin >=2.3.1
